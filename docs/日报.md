# 日报记录

> 维护说明：每天按日期追加新条目（最新在上或按日期倒序），记录当天完成的功能、问题处理、部署与提交情况。提交代码时同步更新本文件。

---

## 2026-08-11

### 一、综合态势大屏优化与部署

1. 完成综合态势顶部用户/密信/安全/签阅指标重组、用户与部门收发排名、业务系统流量展示和多分辨率标题防换行；
2. 完成世界地图卫星图标、北京红星、地面/卫星链路分色、卫星弧线防重叠及整条通信路由悬浮高亮；
3. 将最新前端部署至 `192.168.50.15`，部署标识 `20260811171947`；仅重建 Nginx，后端和 MySQL 未重启；
4. 服务器健康检查为 `UP`，首页返回 200；远端 5 个发布文件与本地产物 SHA-256 全部一致，浏览器验证无资源失败、脚本错误、图表失败或横向溢出；服务器保留上一版 `frontend/dist.previous` 供快速回滚；
5. 修复部署后浏览器可能沿用旧入口脚本并跳转登录页的问题：Nginx 对 SPA HTML 入口设置 `no-store, no-cache, must-revalidate`，带内容哈希的 `/assets/` 静态资源设置一年 immutable 缓存；
6. 将同一版本同步部署至 `192.168.50.12`，部署标识 `20260811172808-12`；仅重建 Nginx，后端和 MySQL 未重启。根地址免登录进入 `/overview`，健康检查、发布哈希和浏览器页面验证均通过，并保留上一版前端供快速回滚；
7. 综合态势底部改为业务流量、安全事件、签阅处置三栏；安全事件增加告警等级、秒级发生时间和处置说明，签阅面板增加处理率及收到/已处理/待处理/异常退回数据；
8. 用户和部门收发排名改为 ECharts 横向排名图，支持总量/发送/接收切换和实时重排，明确展示收发总量、完整 Tooltip 与点击下钻；同步优化业务流量折线和左右统计栏布局；
9. 修复首页业务摘要只显示两条密信登出且下方留白的问题：动态刷新补充密信消息、文件和签阅活动，摘要展示最近四条；业务态势下钻按密信、签阅、系统流量分别展示对应排名，不再复用密信收发排名；
10. 业务态势密信排名改为与首页一致的 ECharts 图（总量/发送/接收切换、序号强调、背景轨道），签阅排名数值改为整数、流量排名保留一位小数；首页顶部指标加大上下留白，数字不再贴近底边框；
11. 排名图表抽取为 `utils/rankingChart.ts` 共享实现，首页与业务态势密信专题的颜色、布局、轴标签与交互完全一致；业务态势指标条与排名面板头部尺寸统一为首页规格，清理旧排名样式残留。

## 2026-08-10

### 一、前端演示态势页完善（综合态势 / 业务态势）

**整体视觉与排版**
- 设计并实现图形化 Logo（替代原“态势”文字标记），移除“安全运营控制台”副标题；
- 结合公司名 SDT 重设计顶栏 Logo：六边形徽章内上方为“SDT”字母组合，下方为雷达态势图形（同心扫描弧 + 扫描线 + 中心亮点），同时体现公司标识与态势感知系统定位；
- 引入 `blackzh` 字体用于系统标题（38px），导航标签 25px，正文统一 `Microsoft YaHei`，字号层级拉开；
- 面板标题统一 20px、内容 18px，移除标题下的冗余副标题；地图点与标签放大，状态颜色语义明确（绿=全员在线、黄=部分在线、灰=全员离线，含人员）。

**布局调整**
- 综合态势页：左栏=全球人员分布+XX装备人员，右栏=今日业务摘要+装备套件状态，中部=地图+境外共享链路+安全事件；
- 业务态势页：仅保留“密信态势/签阅态势”业务专题，移除“境外通联”非业务内容；
- 人员详情改为居中弹层（上移加高、宽扁布局），新增“安全事件”页签，信息密度提升。

**动态实时仿真（核心）**
- 数据全部改为响应式 + 每 10 秒一次的前端仿真：共享链路/业务趋势按真实时钟滚动更新，人员在线/登出自动切换，消息、签阅、文件收发量持续增长，安全事件周期性新增；
- 安全事件与人员双向联动：事件可点击直达人员详情，人员弹窗显示其关联事件及数量徽章；
- 修复事件时间显示小数问题（改为整分钟 + “最新”）；
- 修复 26 名人员姓名缺失导致的 “undefined” 显示问题。

**显示与交互修复**
- 修复各面板内容被裁切、无法下拉的问题（面板弹性布局、列表纵向滚动、图表自适应容器）；
- 所有面板禁止横向滚动，内容超高仅纵向滚动。

### 二、登录与演示模式

- 排查登录 403：确认系统为“三员初始化”机制，重置 sysadmin/secadmin/auditadmin 密码并解锁账户；
- 按演示阶段要求，新增 `VITE_PREVIEW_AUTH` 预览模式：跳过登录、内置演示管理员账户，直接进入演示页面；隐藏演示模式下无效的“系统管理”入口；后续可一键恢复正式登录。

### 三、部署与运维

1. 完成 `192.168.50.15`（Kylin ARM64）最新前端部署，Nginx 重建，首页/健康检查正常，产物哈希核对一致；
2. 确认开机自启（Docker `enabled` + 容器 `unless-stopped`）；
3. 编写端口切换脚本 `change-port.sh`（自动改 compose、放行防火墙、重建并验证），当前对外端口 80；
4. Git：本地提交 `9f05f66`（13 文件 +531/−234）；内网 Git 服务器（192.168.0.20:8081）不可达，已推送到 GitHub 备份，待内网恢复后补推 `origin`。

### 四、待办 / 建议

- 内网 Git 服务器恢复后执行 `git push origin main`；
- 如需恢复正式登录，移除 `.env.integration` 中的 `VITE_PREVIEW_AUTH=preview` 后重新构建部署。
