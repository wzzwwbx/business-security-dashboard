# 日报记录

> 维护说明：每天按日期追加新条目（最新在上或按日期倒序），记录当天完成的功能、问题处理、部署与提交情况。提交代码时同步更新本文件。

---

## 2026-08-13

### 一、离线 Windows 部署支持（前端 node_modules + Maven 离线仓库）

1. 解决"前端在离线 Windows 上运行"问题：Vite 依赖 esbuild/rollup 的平台原生二进制，macOS 的 `node_modules` 不能直接拷到 Windows（缺 `@esbuild/win32-x64`，会报"vite 不是内部或外部命令"）；用 npm ≥ 10.5 的 `--os/--cpu` 跨平台安装生成 win64 版依赖树，并补齐 npm 标准 `.cmd/.ps1` 启动器（`deploy/win-bins.cjs`），实测 `npm run dev:integration` 可用；
2. 后端 Maven 离线仓库：`mvn dependency:go-offline` 有缺陷会漏依赖（实测漏 byte-buddy 等），改用"完整在线构建灌满干净仓库"方式生成 142MB 离线仓库（过滤 5.7GB 全量 `~/.m2`），实测 `mvn -o -DskipTests package` 全离线构建通过；新增一键脚本 `deploy/prepare-node-modules-win64.sh`、`deploy/prepare-m2-offline.sh`；
3. 离线包经 SMB 上传：`node_modules-win64.tar.gz`（32MB）、`m2-repository.tar.gz`（103MB）、后端 jar（30MB）→ `\\192.168.0.90\share\部署\business-security-dashboard-offline\`，SHA-256 均校验一致；
4. 修正 `deploy/windows/README.md` 中"依赖是纯 JS 无原生编译产物"的错误结论，补充 macOS/Linux 侧生成 win64 依赖与 Maven 离线仓库的方法及 Windows 侧应用步骤；
5. 提交并推送 `f2eeefc`（GitHub 与内网 origin 均已同步）。

### 二、最新系统演示截图与 PPT 更新

1. 纠正此前截图为旧 mock 态势页的问题，改为对最新系统首页（`http://localhost:5173/overview` 真实数据仪表盘：在线率 65%、密信收发 82 份、六区域链路、业务系统流量、签阅处置 54/68、安全事件滚动）截图；
2. 新增 15 张 3840×2160 交互截图：首页大盘、管理员下拉菜单、排名切换（发送）、人员详情抽屉四标签（人员概况/装备套件/安全事件/业务活动）、线路安全智能分析 toast、线路拓扑面板、业务专题下钻（message/signing/traffic）、返回综合态势、运维态势、终端态势；
3. 重建演示 PPT（`docs/演示/业务安全态势系统.pptx`，7 页）：封面、综合态势总览、人员详情抽屉·四标签（2×2）、弹出交互（toast + 拓扑面板）、业务态势·专题下钻、运维态势、终端态势；同步刷新 `业务态势.pptx` 单页；新增 `docs/演示/build_ppt.py` 生成脚本可复现；
4. 提交并推送 `e7aa457`、`0545170`（GitHub 与内网 origin 均已同步；GitHub 推送遇 HTTP/2 连接异常 408/重置，改用 `git -c http.version=HTTP/1.1 push` 后成功）；
5. 将 PPT 与截图打包为 `wangbinxu2489-ywts.zip`（24MB，UTF-8 中文文件名、无 `.DS_Store`/`__MACOSX` 等隐藏文件，Windows 解压正常），上传至 `\\192.168.0.90\share\业务态势\`（share 根下独立文件夹，未放"部署"目录），SHA-256 校验一致。

## 2026-08-11

### 一、综合态势大屏优化与部署

1. 完成综合态势顶部用户/密信/安全/签阅指标重组、用户与部门收发排名、业务系统流量展示和多分辨率标题防换行；
2. 完成世界地图卫星图标、北京红星、地面/卫星链路分色、卫星弧线防重叠及整条通信路由悬浮高亮；
3. 将最新前端部署至 `192.168.50.15`，部署标识 `20260811171947`；仅重建 Nginx，后端和 MySQL 未重启；
4. 服务器健康检查为 `UP`，首页返回 200；远端 5 个发布文件与本地产物 SHA-256 全部一致，浏览器验证无资源失败、脚本错误、图表失败或横向溢出；服务器保留上一版 `frontend/dist.previous` 供快速回滚；
5. 修复部署后浏览器可能沿用旧入口脚本并跳转登录页的问题：Nginx 对 SPA HTML 入口设置 `no-store, no-cache, must-revalidate`，带内容哈希的 `/assets/` 静态资源设置一年 immutable 缓存；
6. 将同一版本同步部署至 `192.168.50.12`，部署标识 `20260811172808-12`；仅重建 Nginx，后端和 MySQL 未重启。根地址免登录进入 `/overview`，健康检查、发布哈希和浏览器页面验证均通过，并保留上一版前端供快速回滚；
7. 综合态势底部改为业务流量、安全事件、签阅处置三栏；安全事件增加告警等级、秒级发生时间和处置说明，签阅面板增加处理率及收到/已处理/待处理/异常退回数据；
8. 用户和部门收发排名改为 ECharts 横向排名图，支持总量/发送/接收切换和实时重排，明确展示收发总量、完整 Tooltip 与点击下钻；同步优化业务流量折线和左右统计栏布局；
9. 修复首页业务摘要只显示两条密信登出且下方留白的问题：动态刷新补充密信消息、文件和签阅活动，摘要展示最近四条；业务态势下钻按密信、签阅、系统流量分别展示对应排名，不再复用密信收发排名；
10. 业务态势密信排名改为与首页一致的 ECharts 图（总量/发送/接收切换、序号强调、背景轨道），签阅排名数值改为整数、流量排名保留一位小数；首页顶部指标加大上下留白，数字不再贴近底边框；
11. 排名图表抽取为 `utils/rankingChart.ts` 共享实现，首页与业务态势密信专题的颜色、布局、轴标签与交互完全一致；业务态势指标条与排名面板头部尺寸统一为首页规格，清理旧排名样式残留；
12. 提交并推送 `1cff9cd`（GitHub 与内网 origin 均已同步，origin 恢复可用）；将最新前端部署至 `192.168.50.12`，部署标识 `20260811195842-12`；仅重建 Nginx（`up -d --no-deps --force-recreate nginx`），后端与 MySQL 保持 Up 10 hours 未重启；发布文件 SHA-256 与本地一致，HTML no-store / 资源 immutable 缓存头验证通过，浏览器免登录进入 `/overview`、用户显示“管理员”，业务态势密信专题排名与切换正常；保留上一版 `frontend/dist.previous` 供快速回滚；
13. 实现多跳线路拓扑面板：地图线路保持原样式，点击线路弹出半透明拓扑面板（SVG 展示北京接入中心→路由器/交换机/卫星/信关站→站点多跳链路）；攻击时节点红闪 + 攻击横幅，智能分析生成切换策略后展示策略卡，备用线路以绿色拓扑点亮，攻击解除自动回切；首个 tick 强制触发一次攻击便于演示；
14. 修复卫星线路点不到问题：根因是命中层丢失 curveness、ECharts 贝塞尔命中退化为包围盒（巴西弧线误吞肯尼亚）、geo 控制点公式与屏幕渲染不符、ECharts click 清空面板；最终改用 zr 层自定义命中（convertToPixel 投影端点 + 屏幕公式离散弧线 + 点到折线距离），线路主体可靠可点，悬浮高亮整条路由，点击卫星图标直接打开对应拓扑；
15. 修复悬浮线路着重显示失效：根因是线路可见层改 silent 后 ECharts 不再响应 highlight action；改用 zr 层元素级高亮（按端点标记每条弧线所属路由与基础样式，悬浮时直接改元素线宽/透明度，移出画布 globalout 恢复）；将含多跳拓扑面板与悬浮高亮的最新前端部署至 `192.168.50.12`，部署标识 `20260811210426-12`，SHA-256 与本地一致，健康 UP，HTML no-store / 资源 immutable 缓存头验证通过，浏览器页面正常无脚本错误。

## 2026-08-10

### 一、前端演示态势页完善（综合态势 / 业务态势）

**整体视觉与排版**
- 设计并实现图形化 Logo（替代原“态势”文字标记），移除“安全运营控制台”副标题；
- 结合公司名 SDT 重设计顶栏 Logo：六边形徽章内上方为“SDT”字母组合，下方为雷达态势图形（同心扫描弧 + 扫描线 + 中心亮点），同时体现公司标识与态势感知系统定位；
- 引入 `blackzh` 字体用于系统标题（38px），导航标签 25px，正文统一 `Microsoft YaHei`，字号层级拉开；
- 面板标题统一 20px、内容 18px，移除标题下的冗余副标题；地图点与标签放大，状态颜色语义明确（绿=全员在线、黄=部分在线、灰=全员离线，含人员）。

**布局调整**
- 综合态势页：左栏=全球人员分布+XX装备人员，右栏=今日业务摘要+装备套件状态，中部=地图+境外共享链路+安全事件；
- 业务态势页：仅保留“密信态势/签阅态势”业务专题，移除“境外通联”非业务内容；
- 人员详情改为居中弹层（上移加高、宽扁布局），新增“安全事件”页签，信息密度提升。

**动态实时仿真（核心）**
- 数据全部改为响应式 + 每 10 秒一次的前端仿真：共享链路/业务趋势按真实时钟滚动更新，人员在线/登出自动切换，消息、签阅、文件收发量持续增长，安全事件周期性新增；
- 安全事件与人员双向联动：事件可点击直达人员详情，人员弹窗显示其关联事件及数量徽章；
- 修复事件时间显示小数问题（改为整分钟 + “最新”）；
- 修复 26 名人员姓名缺失导致的 “undefined” 显示问题。

**显示与交互修复**
- 修复各面板内容被裁切、无法下拉的问题（面板弹性布局、列表纵向滚动、图表自适应容器）；
- 所有面板禁止横向滚动，内容超高仅纵向滚动。

### 二、登录与演示模式

- 排查登录 403：确认系统为“三员初始化”机制，重置 sysadmin/secadmin/auditadmin 密码并解锁账户；
- 按演示阶段要求，新增 `VITE_PREVIEW_AUTH` 预览模式：跳过登录、内置演示管理员账户，直接进入演示页面；隐藏演示模式下无效的“系统管理”入口；后续可一键恢复正式登录。

### 三、部署与运维

1. 完成 `192.168.50.15`（Kylin ARM64）最新前端部署，Nginx 重建，首页/健康检查正常，产物哈希核对一致；
2. 确认开机自启（Docker `enabled` + 容器 `unless-stopped`）；
3. 编写端口切换脚本 `change-port.sh`（自动改 compose、放行防火墙、重建并验证），当前对外端口 80；
4. Git：本地提交 `9f05f66`（13 文件 +531/−234）；内网 Git 服务器（192.168.0.20:8081）不可达，已推送到 GitHub 备份，待内网恢复后补推 `origin`。

### 四、待办 / 建议

- 内网 Git 服务器恢复后执行 `git push origin main`；
- 如需恢复正式登录，移除 `.env.integration` 中的 `VITE_PREVIEW_AUTH=preview` 后重新构建部署。

## 后续工作（TODO LIST）

## 后续工作（TODO LIST）

> 记录原则：TODO 仅登记，等明确要求实施后再动手。
> 2026-08-11 第三轮（用户指示实施除部署外全部 TODO）已完成：
> - ⏭️ 第四轮（2026-08-11）按新反馈调整：业务态势区域运行状态**恢复为不可点击**（撤销第三轮“复用多跳拓扑”）；地图拓扑面板**居中显示**；面板**始终同屏展示主/备两条线路**——攻击时被攻击跳红色告警闪烁，切换后主线路整体灰化停用、备线路由“待命”灰色虚线转为“已切换生效”绿色点亮，行标签同步更新（主线路（已切换停用）/ 备线路（已切换生效）），切换前后对比清晰。

> - ✅ 各国国旗位置默认在首都位置：加拿大多伦多→渥太华（-75.7,45.4）、澳大利亚悉尼→堪培拉（149.13,-35.28）、美国洛杉矶→华盛顿（-77.0,38.9），同步城市/坐标/路由种子（路由名同步更新为渥太华/堪培拉/华盛顿）；
> - ✅ 构建分包：echarts 按 core/charts/components/renderers 拆分 + vue-vendor 独立，主包 1.15MB→327KB，全部 chunk <500KB，警告消除；
> - ✅ EChartWidget 清理：移除未使用的 MapChart / VisualMap / Aria / Graphic 模块（geo 仍由 GeoComponent + registerMap 支撑）；
> - ✅ 业务态势接入多跳拓扑：区域运行状态行点击打开拓扑面板（复用 RouteTopologyPanel，覆盖定位避开顶部专题条）；
> - ✅ 回切动画：新增 restoring 过渡态（徽章“回切中”闪烁），攻击解除先回切中再恢复主路由、备用回到待命；
> - ✅ 线路命中阈值随缩放自适应（8px 基准，按 zoom 在 4~12px 间调整）；
> - ✅ 切换策略 toast 单次展示 + 手动关闭按钮；
> - ✅ 多分辨率回归：1536/1366/1280/1024 均无横向溢出，业务态势拓扑面板正常；
> - ⏭️ 部署 .12/.15 待用户确认后执行（本轮按要求未部署）。


1. **各国国旗位置默认在首都位置**：加拿大多伦多 → 渥太华（-75.7, 45.4）、澳大利亚悉尼 → 堪培拉（149.13, -35.28）、美国洛杉矶 → 华盛顿（-77.0, 38.9）；其余站点（北京/阿布扎比/新加坡/柏林/内罗毕/巴西利亚）已是首都，确认后同步城市/坐标/路由种子数据。

> 2026-08-11 已处理：
> - ✅ 所有线路可点可高亮：为离线站点（加拿大/澳大利亚/美国）补充主/备多跳路由，地图展示的全部 11 条线路均参与命中与悬浮高亮；
> - ✅ 卫星高亮错位修复：AE/KE 共用北京→卫-1 上行段按“每条路由占用一个元素”去重分配，高亮阿联酋不再误亮肯尼亚上行段、反之亦然；
> - ✅ 站点图标改为各国国旗（emoji 渲染 SVG data URI，北京保留红星）。

1. **可选优化：构建分包**：处理 `Some chunks are larger than 500 kB after minification` 警告（echarts 按需引入 / manualChunks 拆分 vendor）。
2. **清理 EChartWidget 旧地图扩展**：保留 `getChart()`（供 zr 自定义命中与元素级高亮使用），评估移除不再使用的旧事件/扩展。
3. **业务态势（/business）接多跳拓扑**：目前多跳拓扑仅综合态势地图可用，考虑业务态势区域链路状态点击后复用拓扑面板。
4. **回切动画**：攻击解除回切主路由目前直接恢复，可加"回切中"过渡态与提示。
5. **线路命中阈值自适应**：`HIT_THRESHOLD_PX=8` 为固定值，缩放（roam）后可评估是否随缩放调整，避免高倍缩放时误触/难触。
6. **toast 去重与展示优化**：切换策略 toast 目前同一策略多次触发轮询记录，评估改为单次展示 + 手动关闭。
7. **多分辨率回归**：1366×768 及更小分辨率下拓扑面板宽度（620px）与地图悬浮高亮的完整回归。
8. **待部署 .15**：最新前端（含多跳拓扑、悬浮高亮、线路命中修复）尚未部署至 `192.168.50.15`，需用户确认后同步部署。

### 补充记录（2026-08-11 第二轮）
16. 按反馈修复三项：① 为加拿大/澳大利亚/美国补充主/备多跳路由，地图全部 11 条线路均可点击打开拓扑与悬浮高亮（此前离线站点线路被过滤）；② 修复 AE/KE 共用卫星上行段的高亮错位（元素标记去重，高亮阿联酋不再误亮肯尼亚上行段）；③ 站点点位图标改为各国国旗（emoji → SVG data URI，北京保留红色五角星）。

### 补充记录（2026-08-11 第三轮）
17. 按指示实施除部署外全部 TODO：国旗位置改首都（渥太华/堪培拉/华盛顿）；构建分包消除 chunk 警告；EChartWidget 移除未用 echarts 模块；业务态势区域行点击复用拓扑面板；回切动画（restoring 过渡态）；命中阈值随缩放自适应；策略 toast 单次展示+手动关闭；多分辨率回归通过。

### 补充记录（2026-08-11 第四轮）
18. 按新反馈：撤销业务态势三个专题区域运行状态的拓扑点击；地图拓扑面板居中；拓扑面板始终同屏展示主/备线路（备线路待命灰色虚线 / 切换生效绿色点亮），攻击跳红色告警、切换后主线路灰化停用，行标签标注切换状态，切换前后效果直观可辨。

### 补充记录（2026-08-11 第五轮）
19. 按新反馈重构多跳切换为**分段绕行**：起点、终点固定，中间某跳被攻击时仅该段绕行至替代节点（hop 增加 bypass 绕行节点，route 增加 bypassedHopIndex）；攻击跳红色告警闪烁、绕行节点绿色点亮、其余节点保持不变；拓扑面板展示「主线路（第 X 跳绕行）+ 绕行线路（已生效）」双行，策略卡文案改为分段绕行；回切时恢复主路径并清空绕行；切换策略 toast 支持点击直接打开对应线路拓扑面板。

### 补充记录（2026-08-11 第六轮）
20. 优化分辨率自适应：1280×720 及以上改为压缩满屏（压缩指标条/底部/字号/间距，无整页滚动条），仅 <1280 宽或 <720 高才整页滚动；1366×768、1280×720 首页与业务态势实测满屏无滚动、无横向溢出、底部三栏与排名图完整不裁切。
